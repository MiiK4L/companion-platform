# SPDX-FileCopyrightText: 2026 Companion Platform contributors
#
# SPDX-License-Identifier: Apache-2.0

# Outillage de developpement/CI du socle experimental (choix internes, non
# structurants). Le coeur (paquet measurement/) ne depend d'AUCUN de ces
# paquets : les tests utilisent la bibliotheque standard (unittest).
#
# Version epinglee (reproductible) ; montees gerees par Dependabot.
#
# - ruff : lint + format (choix interne).
# - jsonschema : validateur JSON Schema STANDARD, utilise en CI pour verifier les
#   schemas eux-memes et la PARITE avec le validateur maison. Le coeur (paquet
#   measurement/) ne l'importe pas : la validation maison reste sans dependance.
ruff==0.16.0
jsonschema==4.26.0
