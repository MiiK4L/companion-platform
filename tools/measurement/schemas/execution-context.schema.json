{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Execution context",
  "description": "Metadonnees d'execution (implementation/resultat) : banc, instrument reel, DUT, fixture, build, conditions, etalonnage, baseline. Utiliser \"N/A\" (explicite) pour un champ non applicable ; jamais objet ou chaine vide pour une campagne reelle.",
  "type": "object",
  "additionalProperties": false,
  "required": ["setup", "instrument", "dut", "fixture", "build_manifest", "conditions", "calibration", "baseline"],
  "properties": {
    "setup": { "type": "string" },
    "fixture": { "type": "string" },
    "instrument": {
      "type": "object",
      "additionalProperties": false,
      "required": ["brand", "model", "firmware", "probe", "calibre", "bandwidth", "parameters"],
      "properties": {
        "brand": { "type": "string" },
        "model": { "type": "string" },
        "firmware": { "type": "string" },
        "probe": { "type": "string" },
        "calibre": { "type": "string" },
        "bandwidth": { "type": "string" },
        "parameters": { "type": "string" }
      }
    },
    "dut": {
      "type": "object",
      "additionalProperties": false,
      "required": ["description", "hardware_revision"],
      "properties": {
        "description": { "type": "string" },
        "hardware_revision": { "type": "string" }
      }
    },
    "build_manifest": {
      "type": "object",
      "additionalProperties": false,
      "required": ["git_commit", "git_dirty", "toolchain", "esp_idf_version", "target", "build_configuration_hash", "artifact_sha256", "tooling_version", "measurement_tooling_commit"],
      "properties": {
        "git_commit": { "type": "string" },
        "git_dirty": { "type": "boolean" },
        "toolchain": { "type": "string" },
        "esp_idf_version": { "type": "string" },
        "target": { "type": "string" },
        "build_configuration_hash": { "type": "string" },
        "artifact_sha256": { "type": "string" },
        "tooling_version": { "type": "string" },
        "measurement_tooling_commit": { "type": "string" }
      }
    },
    "conditions": {
      "type": "object",
      "additionalProperties": false,
      "required": ["temperature", "supply", "environment"],
      "properties": {
        "temperature": { "type": "string" },
        "supply": { "type": "string" },
        "environment": { "type": "string" }
      }
    },
    "calibration": {
      "type": "object",
      "additionalProperties": false,
      "required": ["reference", "verified_at"],
      "properties": {
        "reference": { "type": "string" },
        "verified_at": { "type": "string" }
      }
    },
    "baseline": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "record"],
      "properties": {
        "status": { "type": "string" },
        "record": { "type": "string" }
      }
    }
  }
}
